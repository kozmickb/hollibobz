// server/scripts/postinstall.js
// No-op: Prisma generation is handled explicitly in Dockerfile
// Run `npm run generate` locally if needed
console.log("[postinstall] Prisma generation handled in Dockerfile - skipping");
process.exit(0);
